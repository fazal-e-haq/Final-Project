import 'package:flutter/material.dart';

class QuranInfo extends StatelessWidget {
  const QuranInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
