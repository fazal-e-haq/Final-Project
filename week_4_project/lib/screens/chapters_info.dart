import 'package:flutter/material.dart';

class ChaptersInfo extends StatelessWidget {
  const ChaptersInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('hi')

      ],
    );
  }
}
