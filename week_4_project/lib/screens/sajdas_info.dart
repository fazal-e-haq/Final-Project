import 'package:flutter/material.dart';

class SajdasInfo extends StatelessWidget {
  const SajdasInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('hi')

      ],
    );
  }
}
