import 'package:flutter/material.dart';

class RukusInfo extends StatelessWidget {
  const RukusInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('hi')

      ],
    );
  }
}
